/**
 * Digital Pool “Network” tab: only show downline who reached at least this binary level
 * (same rule as dashboard “Level Completed” / L1 unlock today).
 * Raise later (e.g. 2…10) when you add more Digital Pool tiers.
 */
export const MIN_DIGITAL_POOL_NETWORK_BINARY_LEVEL = 1;
